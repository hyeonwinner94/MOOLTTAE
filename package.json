{
  "name": "moolttae-fishing-map",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:web": "node tools/copy-web-assets.mjs",
    "sync:android": "npm run build:web && cap sync android",
    "build:apk": "npm run sync:android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
